{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/xmbshell",
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {
                "XMB_ASSET_DIR": "${workspaceFolder}",
                "GSETTINGS_SCHEMA_DIR": "${workspaceFolder}/schemas",            }
        }
    ]
}
